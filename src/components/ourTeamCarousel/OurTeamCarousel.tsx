import style from "./ourTeamCarouse.module.scss";
import { useTranslation } from "react-i18next";

const OurTeamCarousel: React.FC = () => {
    const {t} = useTranslation();

    return(
        <div className={style.carousel}>

          s
        </div>
    )
}

export default OurTeamCarousel